import * as colors from '../../_colors';

export default {
    color: colors.lightText,
    backgroundColor: colors.primary,
    padding: "5px",
    borderRadius: "2px",
    boxShadow: "0 1px 4px 1px rgba(0,0,0,.5)"
}